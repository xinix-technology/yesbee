// jshint esnext: true

const assert = require('assert');
const co = require('co');
const task = require('../../lib/commands/daemon');
const Execution = require('../../lib/execution');

describe('commands/daemon', function () {
  // it('', function(done) {
  //   co(function *() {
  //     try {
  //       yield task(new Execution({
  //         addComponent() {},
  //         logger() {},
  //       }));
  //       done();
  //     } catch(e) {
  //       done(e);
  //     }
  //   });
  // });
});